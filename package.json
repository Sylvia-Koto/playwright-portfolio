{
  "name": "playwright-portfolio",
  "version": "1.0.0",
  "description": "Playwright test suite showcasing POM, fixtures, CI/CD and Claude MCP integration",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^20.0.0",
    "dotenv": "^17.4.2",
    "typescript": "^5.0.0"
  }
}
